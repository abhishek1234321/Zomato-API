# Zomato API

### Installation
Zomato  requires [Node.js](https://nodejs.org/) to run.

You need Gulp and bower installed globally:

```sh
$ npm i -g gulp bower
```

```sh
$ git clone [git-repo-url] zomato
$ cd zomato
$ npm i
$ bower install
$ gulp
```